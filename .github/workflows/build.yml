name: 构建 Vaultwarden

on:
  repository_dispatch:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps:
    - name: install rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: aarch64-unknown-linux-musl

    - name: 克隆代码
      run: git clone --depth 1 https://github.com/dani-garcia/vaultwarden.git vaultwarden

    - name: 安装aarch64工具链
      run: rustup target add aarch64-unknown-linux-musl&&rustup default aarch64-unknown-linux-musl
        
    - name: 执行编译
      working-directory: vaultwarden
      run: cargo build -r --target aarch64-unknown-linux-musl
